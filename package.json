{
  "name": "sdjs-typescript-template",
  "version": "1.0.0",
  "description": "This is a template repo for smooth-discord.",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "discord.js": "^13.1.0",
    "dotenv": "^10.0.0",
    "smooth-discord": "^1.0.11"
  },
  "devDependencies": {
    "typescript": "^4.3.5"
  }
}
